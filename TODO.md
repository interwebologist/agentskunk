## Future Improvements
- [ ] auxiliary_client is the client compaction uses for the "middle-out" summary (pinned head/tail). this is dup of client agent uses. make dry
- [ ] make model metadata file context (last return statement with ctx) configurable
- [ ] 
